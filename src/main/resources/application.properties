spring.application.name=backend
spring.mongodb.uri=${MONGODB_URI}
commission.ai.base-url=${COMMISSION_AI_BASE_URL:http://host.docker.internal:8000}
jwt.secret=${JWT_SECRET:phoenix-super-secret-jwt-key-must-be-at-least-32-chars-long}
jwt.expiration=${JWT_EXPIRATION:86400000}
