package team.phoenix.backend.audit.domain;

// Tipos de recursos monitorados pelo modulo de auditoria.
public enum AuditResourceType {
    COMMISSION_RATE,
    BRAND,
    STORE,
    POSITION,
    FUNCIONARIO,
    IMPORT
}
